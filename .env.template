# ───── Gmail (Optional) ─────
# Get App Password from https://myaccount.google.com/apppasswords
EMAIL_USER=
EMAIL_APP_PASSWORD=
EMAIL_TO=

# ───── Telegram (Optional) ─────
# Create bot via @BotFather, get chat_id from getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ───── Slack (Optional) ─────
# Incoming Webhook from https://api.slack.com/apps
SLACK_WEBHOOK_URL=

# ───── Google Chat (Optional) ─────
# Webhook from Chat space → Apps & integrations → Webhooks (requires Workspace)
GCHAT_WEBHOOK_URL=
